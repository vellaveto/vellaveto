# Example: Vellaveto policy validation in CI
# Copy this to .github/workflows/policy-check.yml in your repository
name: Policy Validation
on: [pull_request]
jobs:
  check-policies:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: vellaveto/vellaveto/.github/actions/policy-check@main
        with:
          config: vellaveto-config.toml
          strict: 'true'
